create table bells
(
    id serial primary key,
    status          varchar(10),
    calldate        varchar,
    operatorfio    varchar,
    clientphone     varchar,
    contactaudiourl varchar,
    statusbell      varchar
);